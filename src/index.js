import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Se você tiver o Tailwind configurado
import App from './App'; // O componente principal da aplicação

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
